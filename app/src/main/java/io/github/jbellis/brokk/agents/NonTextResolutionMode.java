package io.github.jbellis.brokk.agents;

/** Controls how (and whether) non-text conflicts are resolved automatically. */
public enum NonTextResolutionMode {
    OFF,
    HEURISTICS,
    HEURISTICS_PLUS_AGENT
}
